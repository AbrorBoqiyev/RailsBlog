<%= form_with model: [ article, article.comments.build ]  do |form|%>
    <div class="form-control">
        <%= form.label :commenter %> <br />
        <%= form.text_field :commenter %>
    </div>
    
    <div class="form-control">
        <%= form.label :body %> <br />
        <%= form.text_area :body %>
    </div>

    <div class="form-control">
        <%= form.submit class: "btn btnc btn-outline-primary " %>
    </div>

<% end %>